#include "main.h"
/**
 *_abs - absolute value
 *@n:checking character
 *Return:n if positive
 *if not multipy -n
 *
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else 
	{
		return (-n);
	}
}
